# Keyboard Templates

This directory will contain physical keyboard geometry templates.

Planned initial templates:

- `iso-105.json`
- `ansi-104.json`

A template defines stable physical key IDs, scan-code identity, extended-key state and key geometry in keyboard units. Project-specific character mappings remain in `.kbdproj` files and are not stored in the templates.
