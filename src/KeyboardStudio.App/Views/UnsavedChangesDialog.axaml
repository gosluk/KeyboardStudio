<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="KeyboardStudio.App.UnsavedChangesDialog"
        Title="Unsaved changes"
        Width="460"
        SizeToContent="Height"
        CanResize="False"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
  <StackPanel Margin="24" Spacing="16">
    <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="15" />
    <TextBlock Classes="muted"
               Text="Save your changes before replacing the current document?"
               TextWrapping="Wrap" />
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
      <Button Content="Cancel" Click="CancelClicked" IsCancel="True" />
      <Button Content="Discard" Classes="destructive" Click="DiscardClicked" />
      <Button Content="Save" Classes="primary" Click="SaveClicked" IsDefault="True" />
    </StackPanel>
  </StackPanel>
</Window>
